<Window x:Class="SshManager.Views.GroupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Group"
        Width="420" Height="Auto"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BgDarkBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Style="{DynamicResource SectionTitle}" FontSize="18"/>

        <StackPanel Grid.Row="1" Margin="0,12,0,0">
            <TextBlock Text="Group Name" Style="{DynamicResource FieldLabel}"/>
            <TextBox x:Name="NameBox" Style="{DynamicResource FormTextBox}" Margin="0,0,0,4"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" Style="{DynamicResource SecondaryButton}" Width="100" Margin="0,0,8,0"
                    Click="Cancel_Click" IsCancel="True"/>
            <Button x:Name="SaveButton" Content="Add" Style="{DynamicResource PrimaryButton}" Width="100"
                    Click="Save_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
